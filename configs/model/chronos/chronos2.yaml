# Chronos 2.0 (latest generation)
# Uses predict_df API with DataFrame inputs
name: chronos_2
family: chronos
id: s3://autogluon/chronos-2/
variant: chronos2
torch_dtype: float32
quantile_levels: [0.1, 0.5, 0.9]

# Device compatibility: cuda, mps, cpu
supported_devices: [cuda, mps, cpu]
